# Redis on K8s

## 1. Redis Sentinel

Redis Sentinel is a distributed system designed to provide high availability for Redis deployments. It acts as a monitoring and automatic failover solution for Redis instances configured in a master-replica (aka master-slave)

### 1.1. Replication

### 1.2. Architecture

Redis Sentinel functions as a failover manager, providing high availability for Redis by monitoring instances and automating failover processes when a primary instance fails.

### 1.3. Deploy on K8s

## 2. Redis Cluster